## 结论

已按定点读写契约完成处理：读取 `inputs/conflicting-sources.md`（8 行，成功，无错误），生成并交付 `output/paired/conflict.md`（size 2613，状态 delivered）。source_a 与 source_b 对同一指标 HRV_RMSSD_ms 的冲突已**双值保留 + 待复核标记**，未择一替代。

## 关键内容

- 冲突标识：rehab_id `ATH-003`，timestamp `2026-08-22T08:00:00Z`，指标 `HRV_RMSSD_ms`。
- 原始值逐字保留：source_a（wearable）**51.2 ms**；source_b（ring）**47.8 ms**；差值 3.4 ms。两者单位一致，差异非量纲换算所致。
- 质量状态均标注为「待复核（pending review）」，冲突本身作为显式信息保留。
- 明确写入三条禁止：不得择一作为唯一真值、不得取平均替代原始值、不得静默丢弃任一侧；理由是无任何证据支持某一来源更准确，择一即把未复核判断写成事实，取平均则伪造观测并破坏可追溯性。
- 列出复核清单：设备型号/固件/测量条件可比性、时间戳同步性、各来源信号质量标记、依据设备验证研究或项目校准规则裁决、记录复核人/时间/依据。
- 已声明证据边界：本记录仅依据公开任务输入；本轮未联网检索，对「以哪一来源为准」不作事实性主张。

## 未解决问题与下一步

1. 冲突**尚未裁决**，该 `HRV_RMSSD_ms` 在 ATH-003 时间轴上仍为不可用状态，不得进入基线表、聚合统计或任何结局分析，需在数据集中以缺失/待复核状态排除。
2. 缺少设备验证证据：需另行检索 wearable 与 ring 测量 RMSSD 的一致性/一致性界限研究（Bland–Altman 类），检索结果可作为裁决依据；当前**无直接来源**，本记录不预设结论。
3. 建议下一步：向数据采集方索取 source_a/source_b 的原始波形与质量标记；若无法裁决，按项目规则标记为无法调和冲突并触发重采。裁决后应回到本文件追加复核结论，而非改写原有原始值。
