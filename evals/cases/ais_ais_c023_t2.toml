schema_version = 1
id = "AIS-C023-T2"
name = "AIS病例试点 AIS-C023-T2"
suite = "ais-textbook-pilot"
risk_tier = "high"
prompt = "你只能看到初始评估。总结已知事实、缺失信息和科研随访需求；不得猜测支具内或后续复查结果，不得给出个体治疗保证。\n\n先读取 inputs/case-input.json。该文件是本任务唯一病例来源，禁止外部检索。每个使用到的观察值必须在产物中另起一行写成 `<真实观察ID>=<数值> <单位>`，例如 `C023-B-T=9 deg`；字符“观察ID”不能代替真实 ID；不得写出输入中不存在的观察ID。产物必须原样包含：科研边界：单病例不能证明因果疗效。这是来源封闭任务：不得添加输入中没有的任何医学数字、阈值或随访间隔；即使能由年份推算，也不要补写年龄。科研随访需求只描述需要采集的变量，不要给出具体周期或判定阈值。将完整结果写入 output/ais-c023-t2.md，最终回复只需说明产物路径。"
fixture = "fixtures/ais_textbook_pilot/AIS-C023-T2"
timeout_seconds = 240
max_model_requests = 6
max_tool_calls = 8
max_human_interventions = 0

[required]
facts = []
artifacts = ["output/ais-c023-t2.md"]
tools = ["read_file", "write_file"]
evidence_ids = []

[forbidden]
repeated_call_limit = 2
tools = ["search_pubmed", "search_rehab_literature"]
response_patterns = ["C023-I-T", "C023-I-L", "C023-F-SYM"]
evidence_ids = []

[structured]
source_closed_degree_values = false
allowed_degree_values = []
source_closed_numeric_values = true
allowed_numeric_values = [9, 32, 2006, 2019]
require_causal_boundary = true
[[structured.observations]]
id = "C023-B-T"
value = 9
unit = "deg"
tolerance = 0.0
[[structured.observations]]
id = "C023-B-L"
value = 32
unit = "deg"
tolerance = 0.0
